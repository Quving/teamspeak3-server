pipeline:
  publish:
    image: plugins/docker
    privileged: true
    repo: pingu/ots3
    tags: 3.9.1, latest
    secrets: [ docker_username, docker_password ]
    when:
      status: [ success ]
      branch: [ master ]
